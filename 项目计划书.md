# 消费券合规审核机器人 — 项目计划书

---

## 一、项目概述

### 背景

国家补贴/消费券场景下，需验证用户上传的产品激活照片真实性，防止翻拍、伪造照片骗取补贴。当前每天约 500 单订单需要审核，其中约 100 单需人工介入处理。

### 总体架构：眼睛 · 大脑 · 手脚

系统按功能拆分为三个层次，各司其职：

```
┌─────────────────────────────────────────────────┐
│  ▶  手脚（影刀 RPA）                              │
│      操作电脑、打开订单页面、下载图片、              │
│      调用引擎、读取结果、点击"通过"/"下一条"          │
├─────────────────────────────────────────────────┤
│  ▶  眼睛（OCR + 图像鉴伪 + 引擎模块）              │
│      识别照片中的 SN/IMEI 文字                     │
│      检测照片是否翻拍/拼接/伪造                     │
├─────────────────────────────────────────────────┤
│  ▶  大脑（规则引擎 RuleEngine）                    │
│      综合判断：SN匹配 + 鉴伪结果 + 地址合规          │
│      输出决策：pass（自动通过）/ skip（转人工）       │
└─────────────────────────────────────────────────┘
```

- **手脚（影刀）：** 负责在电脑上操作，它"看"不懂图，但能准确执行指令
- **眼睛（引擎模块）：** 负责"看"，把图片里的文字读出来，判断图片真假
- **大脑（规则引擎）：** 负责"想"，根据眼睛看到的信息，决定这单能不能过

### 预期目标

1. **短期目标：** 每天 500 单中，引擎能自动通过 400 单，剩下 100 单转人工
2. **中期目标：** 通过数据分析，将人工单量从 100 逐步降低
3. **最终形态：**
   - 影刀打开订单 → 下载图片 → 调用引擎
   - 引擎看照片（眼睛）→ 读 SN → 鉴伪 → 规则判断（大脑）
   - pass 的订单 → 影刀自动点击"通过""下一条"
   - skip 的订单 → 影刀自动跳下一单，人工统一处理
4. **底线：** 不放行有问题的订单（涉及国家资金），不确定的宁可靠人工

### 术语

| 术语 | 说明 |
|------|------|
| 国补 | 国家补贴场景（--scene guobu），审核 SN + 鉴伪 + 地址 |
| 非发券 | 非发券场景（--scene no_coupon），额外含身份证识别 |
| SN | 产品序列号，印在产品/包装上的唯一标识 |
| IMEI | 手机国际识别码，15 位数字 |
| skip | 审核结论之一，转人工处理 |
| pass | 审核结论之一，自动通过 |

---

## 二、技术架构

### 技术栈

| 组件 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 运行时 | CPython | 3.12.9 | Windows 环境 |
| OCR | PaddleOCR PP-OCRv4 mobile | 3.5.0 | 本地 CPU 推理，零 API 成本 |
| PaddlePaddle | PaddlePaddle CPU | 3.2.0 | ⚠️ 不能升 3.3.1（PIR bug） |
| 图像处理 | OpenCV | 4.13.0 | 鉴伪分析 |
| 模型缓存 | paddlex models | 23MB | `C:\Users\85169\.paddlex\official_models\` |

### 架构概览

```
┌─────────────┐     ┌──────────────┐     ┌──────────────┐
│  调用入口     │────▶│   引擎核心     │────▶│   JSON 输出   │
│ run_audit.py │     │ modules/     │     │ pass / skip  │
└─────────────┘     └──────────────┘     └──────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
    ┌──────────┐   ┌──────────┐   ┌──────────────┐
    │  OCR 引擎 │   │ 图像鉴伪  │   │  规则引擎     │
    │  SN/IMEI │   │ 翻拍/拼接 │   │  pass/skip   │
    └──────────┘   └──────────┘   └──────────────┘
```

### 模块说明

| 模块文件 | 职责 |
|---------|------|
| `run_audit.py` | 主入口，参数解析，流程编排 |
| `modules/ocr_engine.py` | PaddleOCR 封装，三级识别策略 |
| `modules/code_extractor.py` | SN/IMEI 正则提取 + 精确匹配 |
| `modules/image_forensics.py` | 五维图像鉴伪流水线 |
| `modules/rule_engine.py` | 综合判定规则引擎 |
| `modules/address_checker.py` | 家电地址合规检查 |
| `modules/id_card_parser.py` | 身份证信息提取 |
| `config.py` | 全局配置与阈值 |

---

## 三、审核流程

### 主流程（国补场景）

```
图片输入
    │
    ▼
┌─────────────────────┐
│ 1. 图像鉴伪           │
│    EXIF 分析          │
│    FFT 摩尔纹检测      │
│    ELA 一致性分析      │
│    噪点一致性检测      │
│    Laplacian 清晰度    │
└─────────┬───────────┘
          │
          ▼
┌─────────────────────┐
│ 2. OCR 识别 SN/IMEI  │
│    增强 640px (快速)   │
│    + 分块 960px (补扫) │
└─────────┬───────────┘
          │
          ▼
┌─────────────────────┐
│ 3. SN 精确匹配        │
│    exact / case_only │
│    (不通过→转人工)     │
└─────────┬───────────┘
          │
          ▼
┌─────────────────────┐
│ 4. 地址检查(家电类)    │
└─────────┬───────────┘
          │
          ▼
┌─────────────────────┐
│ 5. 规则引擎判定       │
│    → pass 或 skip    │
└─────────┬───────────┘
          │
          ▼
    JSON 输出决策
```

### OCR 三级策略

| 级别 | 分辨率 | 耗时 | 用途 |
|------|--------|------|------|
| 通用 480px | 480px | ~1s | 常规文字识别 |
| 增强 640px | 640px，unclip=2.0 | ~3s | 快速 SN 定位 |
| 分块 960px | 960px，2×2 分块+10%重叠 | ~17s | 小字 SN 补扫 |

策略：增强 OCR 先跑，未命中再触发分块补扫，避免每单都跑慢路径。

---

## 四、照片合规要求

### 照片数量与顺序（国补场景）

每单上传 **3 张照片**，按固定顺序排列：

| 顺序 | 名称 | 内容要求 | 合规条件 |
|------|------|---------|---------|
| 第1张 | 激活照片 | 手机/产品亮屏 + 包装盒的合照 | 照片中必须清晰可见 SN/IMEI 码（在包装标签上） |
| 第2张 | 产品合照 | 产品与包装盒的远景合照 | 能看出包装与产品的对应关系，两者同时入镜 |
| 第3张 | SN 码照片 | 包装盒 SN 码特写或产品编码面 | SN 码清晰可辨，引擎能通过 OCR 识别 |

### 特殊规则

| 规则 | 说明 |
|------|------|
| 激活照替代拆封照 | 3C 类产品的激活照本身包含了包装+产品合照，可以同时满足第2张要求 |
| 家电类灵活性 | 家电类第2张只需产品合照，第3张只要有 SN 码即可，包装形式不限 |
| 照片不得翻拍 | 所有照片不能是屏幕翻拍/截图/打印后再拍，引擎通过 FFT 摩尔纹+噪点分析检测 |

### 翻拍检测标准

| 检测项 | 判定标准 | 说明 |
|--------|---------|------|
| FFT 摩尔纹 | >0.83 | 屏幕翻拍会产生周期性纹路 |
| 噪点一致性 | >4.2 | 拼接/二次翻拍区域的噪点分布异常 |
| Laplacian 清晰度 | <80 | 模糊/对焦不准的照片 |
| EXIF 分析 | 无相机信息 | 非原始拍摄（截图、下载图） |
| 边缘均匀度 | 水平/垂直比 >2.5 | 屏幕扫描线导致水平边缘异常多 |

### 注意事项

- 照片顺序由用户上传决定，引擎不强制校验顺序（用户可能把激活照传到第2张位置）
- 当前引擎对所有照片统一做 OCR + 鉴伪，**不区分每张照片的角色**，后续可优化为按位置单独校验
- 3C 类与家电类的照片要求有差异，引擎通过 `system_data.product_type` 判断类别后走不同分支

---

## 五、当前实现状态

### 调用方式

```bash
# 方式一：引擎下载图片
python run_audit.py --scene guobu --image_urls "url1|url2|url3" --system_data '{"sn":"xxx","order_id":"xxx"}'

# 方式二：本地图片目录
python run_audit.py --scene guobu --images_dir ./photos --system_data '{"sn":"xxx"}'

# 方式三：请求文件
python run_audit.py --request_file request.json
```

### 决策输出

```json
{
  "decision": "pass/skip/engine_error",
  "skip_reason": null,
  "scene": "guobu",
  "codes": {
    "sn": { "sn_match": true, "found_sns": [...], "match_type": "exact" },
    "imei": { ... }
  },
  "image_forensics": {
    "status": "pass/suspicious",
    "per_image": [...]
  },
  "rules": { ... }
}
```

### 已删除的冗余模块

| 文件 | 原因 |
|------|------|
| `modules/photo_classifier.py` | 照片不含描述性文字，关键词分类不可行 |

---

## 六、测试结果

### SN 识别测试（30 张手机激活照）

| 指标 | 数值 |
|------|------|
| 测试集 | 30 张 iPhone 17 Pro Max 激活照 |
| 精确匹配命中 | 26/30 = 87% |
| 排除故意放错后 | 26/28 = 93% |
| 增强 OCR 直接命中 | ~28% |
| 分块 OCR 补扫命中 | ~59% |
| 平均耗时 | ~15s/张 |
| 总耗时 | ~450s/30张 |

### 失败案例分析

| 图片 | 原因 | 处理 |
|------|------|------|
| GHKQ6RQP19 | SN 在包装标签第二行极小字，OCR 未识别 | 转人工 |
| K9JGC425R4 | 同上，极小字未识别 | 转人工 |
| 5JV0226225026235 | 用户故意放入的错误照片 | 正确拦截 ✅ |
| CCP425PX72 | 用户改了一个字母测试，旧版编辑距离误放行 | **已修复** ✅ |

### 版本变更记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-05 | v1.0 | 基础 OCR + 鉴伪 + pass/skip 决策 |
| 2026-05 | v1.1 | 删除 photo_classifier，项目瘦身 |
| 2026-05 | v1.2 | 测试 30 张激活照，命中率 93% |
| 2026-05 | v1.3 | 移除编辑距离模糊匹配，改为仅精确匹配 |

---

## 七、图像鉴伪能力

### 五维鉴伪流水线

| 检测项 | 方法 | 说明 |
|--------|------|------|
| EXIF 分析 | 检查相机原始信息 | 无 EXIF/编辑软件标记 → 可疑 |
| FFT 摩尔纹 | 频域分析 | >0.83 判定为屏幕翻拍 |
| 边缘均匀度 | 水平/垂直边缘比 | 翻拍屏幕的边缘模式异常 |
| ELA 一致性 | Error Level Analysis | >0.10 判定为图像编辑 |
| 噪点一致性 | 局部方差分析 | >4.2 判定为拼接 |
| Laplacian 清晰度 | 方差检测 | <80 判定为模糊 |

### 当前限制

正常图片误报率偏高（测试中 6/19 张正常图被标异常），需要进一步调优阈值。

---

## 八、后续优化计划

### Phase 1：CSV 报表输出

新增 `--report` 参数，将每单审核结果追加写入 CSV 文件。

```csv
时间,订单号,场景,决策,原因,SN匹配,鉴伪异常,地址异常,图片数
```

方便每日汇总统计转人工原因分布，指导后续阈值调优。

### Phase 2：鉴伪阈值调优（进行中）

目标：将正常图片误报率从 ~30% 降到 ~10%。

方法：对照 CSV 报表中"鉴伪异常"占比，逐步调优 FFT/噪点/清晰度阈值。

### Phase 3（远期规划）

- 图片内容识别（零样本分类模型如 CLIP）
- 多帧投票提高小字 SN 识别率

---

## 九、环境与部署

### 依赖安装

```bash
pip install paddlepaddle==3.2.0 -f https://www.paddlepaddle.org.cn/whl/windows/mkl/avx/stable.html
pip install paddleocr==3.5.0
pip install opencv-python==4.13.0
```

模型首次运行时自动下载到 `C:\Users\85169\.paddlex\official_models\`（约 23MB）。

### 注意事项

| 事项 | 说明 |
|------|------|
| 编码 | 所有 .py 文件必须含 `# -*- coding: utf-8 -*-`（Windows cp936） |
| 存储 | 临时图片用完即删，日志 10MB 自动轮转 |
| 退出码 | pass/skip → exit 0，engine_error → exit 3 |
| 进程 | 每次调用独立进程，跑完即退，不累积内存 |

---

*文档生成日期：2026-05-20*
