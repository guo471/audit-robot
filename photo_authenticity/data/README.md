# Local Data Contract

All data stays local. Inputs are staged under `data/input/`; versioned manifests and splits are generated under `data/manifests/` and `data/splits/`. SHA-256, human label status, source-group basis and evidence are mandatory. `weak_label` is exploratory only. Locked and challenge memberships are immutable across incremental versions.
