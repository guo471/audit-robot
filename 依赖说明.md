# 消费券合规审核机器人 — 依赖与部署说明

> 本文档记录项目的所有依赖位置和大小，方便迁移到新电脑时快速重建环境。

---

## 一、项目文件

| 项目 | 路径 | 大小 |
|------|------|------|
| 项目根目录 | `C:\audit_robot\` | 25 MB |
| 主入口 | `C:\audit_robot\run_audit.py` | — |
| 配置文件 | `C:\audit_robot\config.py` | — |
| 测试图片 | `C:\audit_robot\test_samples\` | ~24 MB（25 张） |

### 模块文件

| 文件 | 说明 |
|------|------|
| `modules/ocr_engine.py` | PaddleOCR 封装（通用模式 480px + 增强模式 640px） |
| `modules/rule_engine.py` | 规则引擎（SN/IMEI 比对 + 地址合规） |
| `modules/image_forensics.py` | 图像鉴伪（EXIF → FFT → ELA → 噪点 → 清晰度 → 翻拍检测） |
| `modules/code_extractor.py` | SN/IMEI 提取 |

> **迁移提示**：直接拷贝 `C:\audit_robot\` 整个目录即可，代码中都是相对路径引用模块，不需要修改路径。

---

## 二、Python 环境

### 安装包

| 包 | 版本 | 大小 |
|----|------|------|
| Python 3.12.9 | AMD64 | ~1.1 GB（含全部 site-packages） |
| 安装路径 | `C:\Users\85169\AppData\Local\Programs\Python\Python312\` | |
| Python 可执行文件 | `C:\Users\85169\AppData\Local\Programs\Python\Python312\python.exe` | |

> **迁移提示**：Python 安装包保留在 `C:\Users\85169\Downloads\python-3.12.9-amd64.exe`，也可以重新从官网下载：
> https://www.python.org/downloads/release/python-3129/
> **安装时务必勾选 "Add Python to PATH"**。

### pip 依赖包

| 包 | 版本 | 说明 |
|----|------|------|
| `paddlepaddle` | **3.2.0** | ⚠️ 不能装 3.3.1（有 PIR bug），必须锁定 3.2.0 |
| `paddleocr` | 3.5.0 | |
| `paddlex` | 3.5.2 | PaddleOCR 内部依赖 |
| `opencv-python` | 4.13.0.92 | 图像处理 |
| `opencv-contrib-python` | 4.10.0.84 | 可选，图像处理 |
| `numpy` | **2.3.5** | ⚠️ 不能装 numpy 2.4+（Paddle 还不兼容） |
| `pillow` | 12.2.0 | 图像处理 |
| `piexif` | 1.1.3 | EXIF 读取 |

**安装命令**（已配置清华镜像源）：

```bash
pip install paddlepaddle==3.2.0 paddleocr opencv-python numpy==2.3.5 pillow piexif -i https://pypi.tuna.tsinghua.edu.cn/simple
```

> **迁移提示**：不需要手动一个个装，运行 `setup.bat` 一键安装全部依赖。
> 离线迁移方案：打包 site-packages 比较困难（384MB paddle + 依赖体积大），建议新电脑直接在线安装。

---

## 三、OCR 模型缓存

PaddleOCR 首次运行时会自动下载模型到缓存目录。缓存位置和当前占用：

```
C:\Users\85169\.paddlex\official_models\
├── PP-OCRv4_mobile_det/          4.8 MB    文本检测模型
├── PP-OCRv4_mobile_rec/          11 MB     文本识别模型
└── PP-LCNet_x1_0_textline_ori/   6.6 MB    文本行方向分类
──────────────────────────────────────────
总计                               23 MB
```

### 已清理的模型

以下模型已删除（保留会占用额外 ~212 MB，影响不大但没必要）：

| 模型 | 大小 | 删除原因 |
|------|------|----------|
| PP-OCRv5_server_det | 47 MB | v4 mobile 更快足够 |
| PP-OCRv5_server_rec | 99 MB | v4 mobile 更快足够 |
| UVDoc | 31 MB | 文档展平不需要 |
| PP-LCNet_x1_0_doc_ori | 19 MB | 方向分类不需要 |
| en_PP-OCRv4_mobile_rec | 16 MB | 中文模型更好 |

> **迁移提示**：模型缓存目录 `C:\Users\85169\.paddlex\` 可以整体打包拷贝到新电脑的相同位置，这样新电脑就不需要重新下载模型。也可以让 setup.bat 自动触发下载（首次运行会自下载）。

---

## 四、一键部署

项目已经准备好一键部署脚本 `setup.bat`。在新电脑上只需：

1. 安装 Python 3.12.9
2. 运行 `setup.bat`

`setup.bat` 会自动完成：
- 检测 Python 环境
- 安装 pip 依赖（清华镜像源 → 失败则自动换官方源）
- 下载 OCR 模型（首次运行自动下载）
- 验证所有模块可正常导入

---

## 五、测试脚本

| 脚本 | 说明 | 运行方式 |
|------|------|----------|
| `setup.bat` | 一键部署环境 | 双击运行 |
| `OCR测试.bat` | 测试 OCR 识别伪造图片 | 双击运行 |
| `验证引擎.bat` | 验证所有模块导入 | 双击运行 |
| `run_audit.bat` | 启动完整审核流程 | 双击运行 |

---

## 六、引擎版本摘要

| 组件 | 技术方案 | 版本 |
|------|----------|------|
| OCR | PaddleOCR + PP-OCRv4 mobile | v3.5.0 |
| 深度学习框架 | PaddlePaddle CPU | v3.2.0 |
| 图像处理 | OpenCV | v4.13.0 |
| 图像鉴伪 | FFT + ELA + 噪点 + Laplacian | 自研 |

---

*最后更新：2026-05-18*
