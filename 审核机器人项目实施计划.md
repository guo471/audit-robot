# 嘉惠多多审核机器人项目实施计划

> 版本：v0.1  
> 日期：2026-05-20  
> 范围：家电数码/3C 审核优先，汽车审核暂作为后续独立方向  
> 原则：只自动通过高置信合格订单，不自动驳回；不确定订单交由人工处理

---

## 1. 项目背景

当前审核工作主要在网页审核后台完成，人工每天重复查看订单字段、上传图片和审核记录。以非发券 3C 场景为例，审核员需要打开图片，比对 SN 码，查看身份证姓名和有效期，并判断商品图片是否为实拍。业务实际情况中，约 700 单里通常只有十几单不合格，绝大多数订单最终可以通过。

现有项目已经完成了初步的“眼睛”和“大脑”：

- 眼睛：本地 PaddleOCR、SN/IMEI 提取、身份证解析、图像鉴伪。
- 大脑：规则引擎，输出 `pass / skip / engine_error`。
- 手脚：影刀 RPA 仍未正式接入，后续也可替换为 Playwright 等浏览器自动化方案。

本项目的目标不是替代人工做驳回，而是安全地筛出“绝对能通过”的订单，提高整体审核效率，降低重复劳动。

---

## 2. 业务范围与边界

### 2.1 首期优先场景

首期优先覆盖非发券 3C 手机数码审核，原因：

- 页面字段结构相对固定。
- 图片角色清晰，例如身份证人像面、身份证国徽面、新物照片、新商品拆封激活照片。
- 关键规则明确：姓名一致、身份证有效期有效、SN 匹配、商品图实拍。
- IMEI 不属于强制要求，作为辅助证据，不作为自动通过硬门槛。

### 2.2 新增确认：国补家电数码 3C 场景

根据补充截图，国补页面与非发券页面存在明显差异：

- 页面路径为“家电数码3C（国补2026）/申请详情”。
- 左侧为系统字段、订单信息、结算信息。
- 右侧上传图片通常为：
  - 商品照片
  - 拆封照片
  - SN码采集/激活照片
- 国补页面截图中没有身份证人像面和身份证国徽面，因此国补不应套用非发券身份证姓名/有效期规则。
- 品类覆盖手机、电脑、电视机、电冰箱等。
- 字段包含类型、商品名称、品牌、规格型号、价格、补贴金额、商品条码、能效标识、SN码、IMEI1/IMEI2、所在商户、配送方式、收货地址、渠道订单号、交易日期、发票编号、发票照片、结算信息等。
- 国补家电数码为交叉审核队列，点击“下一条”后可能从家电切换到 3C，也可能从 3C 切换到家电，因此不能按队列固定品类处理，必须每一单重新读取页面字段并动态分类。
- 能效和发票当前不纳入机器人判断范围，机器人只记录相关字段或系统状态，不因为能效/发票自动通过或转人工。

因此项目需要同时支持两条首期业务线：

| 场景 | 首期目标 | 自动通过关键条件 |
|---|---|---|
| 非发券 3C | 优先实现 | 身份证姓名、身份证有效期、SN、商品图实拍 |
| 国补家电数码 3C | 同步纳入规划，分批实现 | SN、商品/拆封/激活照片实拍、品类规则、家电地址/能效辅助判断 |

国补自动通过应按每单品类动态判断。首期可先覆盖“页面字段完整、SN 明确匹配、商品/拆封/SN码采集图片无强风险”的标准单；涉及地址灰区、商户特殊规则、图片角色异常的订单先进入人工。能效和发票不作为首期机器人判断项。

### 2.3 首期暂不自动处理的场景

以下场景首期不建议自动通过，统一进入人工：

- 页面字段读取不完整。
- 图片角色无法判断。
- SN 未识别或疑似不一致。
- 身份证姓名或有效期无法高置信识别。
- 商品图片疑似非实拍、截图、网图、拼图、P 图。
- 家电地址存在灰区。
- 历史驳回后重新提交、疑似资料篡改。
- 汽车审核。
- 国补页面字段异常、图片角色异常或地址需要人工解释。

### 2.4 汽车审核边界

汽车审核涉及车辆照片、行驶证、车牌号、车辆类型、所有人、使用性质、车架号、银行卡、开户行、POS 小票、贷款合同、完税证明、活动差异等，规则复杂且活动差异大。汽车审核不纳入首期自动通过范围，后续应作为独立模块设计。

---

## 3. 核心原则

1. 只自动通过，不自动驳回。
2. 不确定即人工。
3. IMEI 作为辅助信息，不作为非发券 3C 自动通过硬条件。
4. 身份证图片不做实拍鉴伪。
5. 商品/拆封/激活图片必须满足实拍要求。
6. 日志保留嘉联订单号，便于复查。
7. 日志不保存身份证号、手机号、图片 URL、OCR 原文等敏感信息。
8. OCR 和图像判断全部本地运行，不上传第三方 API。

---

## 4. 目标指标

### 4.1 首期目标

| 指标 | 目标 |
|---|---:|
| 单日处理量 | 700 单 |
| 自动通过率 | 试运行阶段先测真实基线；第一版现实目标 70%-85%，数据充分后再评估是否能冲 85%+ |
| 自动驳回率 | 0% |
| 误通过率 | 目标接近 0，发现后立即收紧规则 |
| 700 单耗时 | 单机 CPU 方案第一版按 4-8 小时预估；完成服务化和快路径优化后再争取 2-4 小时 |
| 数据外传 | 0 |

### 4.2 验收标准

首期验收不以“识别所有问题单”为目标，而以“安全通过确定合格单”为目标：

- 连续试跑不少于 3 天。
- 每天保留脱敏报表。
- 人工抽查自动通过订单。
- 如出现误通过，必须定位原因并调整规则。
- 自动通过率达到实际业务可接受水平后，再进入稳定运行。

---

## 5. 系统架构

推荐架构：

```text
网页审核后台
  |
  | 读取页面字段、图片标题、图片 URL
  v
手脚层：影刀 RPA 或 Playwright
  |
  | 下载图片到本地临时目录
  | 请求本机审核服务
  v
本地审核服务 127.0.0.1:8765
  |
  | OCR / 身份证解析 / SN 匹配 / 实拍判断 / 规则判断
  v
返回审核结论
  |
  +-- pass   -> 手脚层点击“通过”
  +-- manual -> 手脚层点击“下一条”
  +-- error  -> 手脚层暂停或点击“下一条”，按配置处理
```

### 5.1 为什么采用本地服务

当前命令行方式每单启动一次 Python 进程，会重复加载 OCR 模型，耗时较高。改为常驻本地服务后，模型只在服务启动时加载一次，影刀或 Playwright 通过 HTTP 调用即可。

服务必须绑定本机地址：

```text
127.0.0.1:8765
```

禁止监听 `0.0.0.0`，避免局域网访问。

---

## 6. 首期审核规则设计

### 6.1 页面字段

首期需要从网页读取：

| 字段 | 用途 |
|---|---|
| 嘉联订单号 | 日志索引、复查 |
| 渠道订单号 | 国补页面订单索引，可辅助复查 |
| 活动名称 | 判断场景 |
| 姓名 | 与身份证 OCR 姓名比对 |
| 品类类型 | 判断 3C/家电/其他 |
| SN 码 | 强制匹配 |
| IMEI1/IMEI2 | 辅助匹配，不作为首期硬门槛 |
| 商品名称/品牌/型号 | 可作为辅助，不作为首期硬门槛 |
| 价格/补贴金额 | 可用于报表和异常检查，首期不做硬判 |
| 商品条码 | 后续可用于条码识别辅助 |
| 能效标识 | 仅记录，不作为首期机器人判断项 |
| 所在商户 | 特殊商户规则和复查 |
| 配送方式 | 自提/配送，辅助家电场景 |
| 收货地址 | 家电地址规则 |
| 发票编号/发票照片 | 仅记录，不作为首期机器人判断项 |
| 结算状态/结算信息 | 首期记录，不作为自动通过核心条件 |
| 图片标题 | 判断图片角色 |
| 图片 URL | 下载图片，本地临时使用，不写日志 |
| 系统绿勾状态 | 仅记录和观察，首期不作为决定性条件 |

### 6.2 图片角色

不能按图片顺序硬判断，应优先按页面图片标题判断。

| 图片标题特征 | 角色 | 处理 |
|---|---|---|
| 身份证人像面 | `id_front` | OCR 姓名 |
| 身份证国徽面 | `id_back` | OCR 有效期 |
| 新物照片/商品照片 | `product_photo` | 实拍检测，可辅助 SN |
| 拆封照片 | `unboxing_photo` | 实拍检测，判断商品和包装关系 |
| 激活照片/SN码采集/序列号 | `activation_photo` | SN OCR，实拍检测 |
| 无法识别 | `unknown` | 人工 |

### 6.3 非发券 3C 自动通过条件

同时满足以下条件才自动 `pass`：

1. 页面字段读取成功。
2. 嘉联订单号存在。
3. 品类识别为 3C/数码/手机等首期支持品类。
4. 存在身份证人像面、身份证国徽面、商品或激活图片。
5. 身份证人像面 OCR 到姓名，且与系统姓名一致。
6. 身份证国徽面 OCR 到有效期，且未过期。
7. 商品/激活图片 OCR 到系统 SN。
8. 商品/激活图片无强非实拍风险。
9. 未触发 P 图、截图、网图、拼图等强风险。
10. 服务执行无异常。

不满足任意条件则 `manual`，手脚层点击“下一条”。

### 6.4 IMEI 处理

IMEI1/IMEI2 不作为非发券 3C 自动通过硬条件。建议处理方式：

- 如果识别到且一致：作为辅助证据写入内部结果。
- 如果识别不到：不阻断自动通过。
- 如果识别到明显与系统不一致：首期建议 `manual`，不自动通过。

这样既符合“IMEI 非强制”的业务规则，又避免识别到明显冲突时误放。

### 6.5 家电规则方向

家电暂不作为首期自动通过主场景。后续规则方向：

- SN 来源为机身或外包装。
- 商品/拆封图片需实拍。
- 家电地址需要符合门牌/组/村/栋等细化要求。
- 能效/水效存在灰区时转人工。
- 家电 SN 的横杠、系统补 `00` 等特殊情况需要单独规则。

### 6.6 国补家电数码动态审核规则方向

国补场景不应套用非发券的身份证规则。由于国补家电数码为交叉审核队列，每点击下一条都可能切换品类，因此必须逐单读取“类型/商品名称/品牌/规格型号”等字段，动态判断本单是 3C、电脑、电视、冰箱或其他家电，再进入对应分支。

国补统一前置条件：

1. 成功读取页面字段和图片标题。
2. 成功识别品类分支。
3. 至少存在商品照片、拆封照片、SN码采集/激活照片。
4. 商品/拆封/SN码采集图片无强非实拍风险。
5. 图片无明显网图、电子屏、截图、拼图、P 图。
6. SN 在商品/拆封/SN码采集图片中明确匹配系统 SN。

能效和发票不参与机器人自动判断。

#### 6.6.1 国补手机/电脑/数码 3C

同时满足以下条件才可自动 `pass`：

1. 场景识别为国补家电数码，且品类分支为 3C/电脑/手机等。
2. 品类属于首期支持的 3C/电脑/手机等。
3. 页面关键字段读取成功：姓名、品类、商品名称、品牌、价格、SN、渠道订单号。
4. 页面字段无明显异常。
5. 图片角色识别成功，至少存在商品照片、拆封照片、SN码采集/激活照片。
6. SN码采集/激活照片或商品/拆封图中 OCR 到系统 SN。
7. 商品照片、拆封照片、SN码采集/激活照片无强非实拍风险。
8. 图片不重复、不明显为网图、电子屏、截图、拼图、P 图。
9. IMEI 仍作为辅助信息，不作为首期硬门槛；若识别到明显冲突，进入人工。

不满足任意条件则 `manual`。

#### 6.6.2 国补电视/冰箱等家电

家电场景需要额外谨慎，首期建议仅自动通过非常标准的订单：

1. 品类识别为电视、冰箱、洗衣机、空调、热水器等已支持家电。
2. 商品照片、拆封照片、SN码采集照片均存在。
3. SN码采集照片或铭牌/外包装中 OCR 到系统 SN。
4. 商品/拆封/SN采集图片无强非实拍风险。
5. 收货地址字段存在，且可识别为较详细地址。
6. 对于“地址是否足够细”“店铺样品是否可过”“商品/拆封关系是否合理”等灰区，进入人工。

家电首期不建议追求过高自动通过率，应先通过报表观察常见 manual 原因后再逐步放宽。能效不作为首期判断项。

---

## 7. 技术实现方案

### 7.1 本地审核服务

技术选型：

- Python 3.12
- FastAPI 或 Flask
- PaddleOCR
- OpenCV
- Pillow
- piexif

推荐接口：

```http
POST http://127.0.0.1:8765/audit
Content-Type: application/json
X-Audit-Token: <本机随机密钥>
```

请求示例：

```json
{
  "jl_order_no": "471167761709915068825678",
  "scene": "no_coupon",
  "product_type": "手机",
  "name": "黄某某",
  "sn": "3AX0225C24014734",
  "imei1": "868346086951264",
  "imei2": "868346086964267",
  "images": [
    {"title": "二代居民身份证人像面", "path": "temp/order_x/id_front.jpg"},
    {"title": "二代居民身份证国徽面", "path": "temp/order_x/id_back.jpg"},
    {"title": "新物照片", "path": "temp/order_x/product.jpg"},
    {"title": "新商品拆封激活照片", "path": "temp/order_x/activation.jpg"}
  ]
}
```

响应示例：

```json
{
  "decision": "pass",
  "action": "approve",
  "manual_reason": null,
  "jl_order_no": "471167761709915068825678",
  "evidence": {
    "name_match": true,
    "id_valid": true,
    "sn_match": true,
    "real_photo_pass": true,
    "imei_observed": false
  },
  "elapsed_sec": 8.6
}
```

若不满足自动通过：

```json
{
  "decision": "manual",
  "action": "next",
  "manual_reason": "SN未高置信识别",
  "jl_order_no": "471167761709915068825678",
  "elapsed_sec": 12.4
}
```

### 7.2 手脚层方案

#### 方案 A：影刀 RPA

优点：

- 上手快。
- 适合固定网页流程。
- 可以调用本地脚本或本地服务。
- 对非开发人员较友好。

风险：

- 授权费用需向影刀确认。
- 云同步、运行日志、截图留存等数据安全策略需确认。
- 页面改版后流程可能失效。

适用建议：

- 第一版可用影刀快速验证。
- 仅调用本机 `127.0.0.1` 服务。
- 不使用云端 OCR 或外部 API。

#### 方案 B：Playwright Python

优点：

- 免费。
- 浏览器自动化稳定。
- 数据流完全可控。
- 更适合长期工程化。

风险：

- 开发成本高于影刀。
- 登录态、验证码、页面异常处理需要编码实现。

适用建议：

- 若影刀收费高或安全策略无法满足要求，可切换 Playwright。
- 因为审核服务接口稳定，切换手脚层不影响眼睛和大脑。

---

## 8. 数据安全方案

### 8.1 安全原则

1. 全流程本地化。
2. 不上传第三方 OCR、大模型、网盘或公网接口。
3. 临时图片用完即删。
4. 日志保留嘉联订单号，但不保留高风险个人信息。
5. 服务仅监听 `127.0.0.1`。
6. 接口加本地 token。

### 8.2 日志字段

允许完整保留：

- 嘉联订单号
- 审核时间
- 审核结论
- 场景
- 品类
- 图片数量
- 图片角色
- 耗时
- manual 原因

不建议保存：

- 完整身份证号
- 完整手机号
- 银行卡号
- 图片 URL
- OCR 原文
- 身份证图片
- 发票图片
- 行驶证图片

SN/IMEI 建议默认脱敏或只保存匹配状态。

### 8.3 CSV 报表示例

```csv
time,jl_order_no,scene,product_type,decision,manual_reason,sn_match,id_name_match,id_valid,real_photo_pass,image_count,elapsed_sec
2026-05-20 13:20:01,471167761709915068825678,no_coupon,手机,pass,,true,true,true,true,4,8.6
2026-05-20 13:21:14,471167761709915068825679,no_coupon,手机,manual,SN未高置信识别,false,true,true,true,4,12.4
```

---

## 9. 开发计划

### Phase 0：现状整理与安全加固

预计用时：0.5-1 天

任务：

1. 明确日志字段。
2. 增加脱敏工具。
3. 禁止日志输出 OCR 原文和图片 URL。
4. 保留嘉联订单号作为复查索引。

预期结果：

- 当前命令行引擎不再输出敏感原文到日志。
- 具备安全报表基础。

### Phase 0.5：最小闭环联调

预计用时：1-2 天

任务：

1. 用 1 条真实订单走完整链路：页面读取字段、下载图片、调用本地引擎、返回 JSON、记录报表。
2. 暂不点击通过，只验证数据链路和结果格式。
3. 验证影刀或 Playwright 是否能稳定读取：
   - 嘉联订单号/渠道订单号
   - 类型
   - 姓名
   - SN 码
   - 图片标题
   - 图片地址或下载后的本地路径
4. 验证异常处理：
   - 图片下载失败
   - 服务超时
   - JSON 解析失败
   - 页面元素读取失败

预期结果：

- 证明“手脚层 -> 本地引擎 -> 报表”的最小闭环可行。
- 若此阶段不稳定，先不继续扩大规则范围。

### Phase 1：图片角色分类与规则重构

预计用时：1-2 天

任务：

1. 新增图片角色分类模块。
2. 按图片标题识别身份证人像面、国徽面、商品图、拆封图、SN码采集/激活图。
3. 身份证图片跳过实拍检测。
4. 商品/激活图片执行 SN OCR 和实拍检测。
5. IMEI 改为辅助证据。
6. 新增国补图片角色规则，不再要求身份证图片。
7. 国补每单动态判断品类，不能沿用上一单分类结果。

预期结果：

- 非发券 3C 规则更贴近实际业务。
- 避免把身份证非实拍误判为订单风险。
- 自动通过条件更合理。

### Phase 2：本地审核服务

预计用时：1-2 天

任务：

1. 新增 `audit_service.py`。
2. 使用 FastAPI 或 Flask 提供 `/audit` 接口。
3. 服务启动时预加载 OCR 模型。
4. 仅绑定 `127.0.0.1`。
5. 增加 `X-Audit-Token` 校验。

预期结果：

- 影刀或 Playwright 可通过 HTTP 调用引擎。
- 减少每单重复加载 OCR 的耗时。

### Phase 3：报表与试跑

预计用时：2-4 天

任务：

1. 增加 CSV 报表。
2. 记录嘉联订单号、结论、原因、耗时、图片角色、关键匹配状态。
3. 先进行影子试跑：真实订单只记录建议结果，不自动点通过。
4. 至少积累 100-200 单真实样本后再评估自动通过率和耗时。
5. 人工抽样核对自动 `pass` 的准确性。

预期结果：

- 得到真实自动通过率。
- 得到 manual 原因分布。
- 明确后续优化重点。
- 避免在没有真实数据验证前直接上线自动点击。

### Phase 4：手脚层接入

预计用时：1-3 天

任务：

1. 影刀读取页面字段。
2. 影刀读取图片标题与图片地址。
3. 下载图片到本地临时目录。
4. 请求本地审核服务。
5. 根据结果执行：
   - `pass`：点击通过。
   - `manual`：点击下一条。
   - `engine_error`：暂停或下一条，按配置处理。
6. 设置超时和异常兜底。

预期结果：

- 形成完整自动通过流程。
- 不自动驳回。
- 不确定订单留给人工。

### Phase 5：优化自动通过率

预计用时：持续优化，首轮 2-5 天

任务：

1. 根据报表统计 manual 原因。
2. 优先优化占比最高的原因。
3. 加入条码/二维码识别辅助 SN。
4. 调整强/弱实拍风险规则。
5. 优化身份证有效期 OCR。
6. 基于国补样本分别统计手机、电脑、电视、冰箱的 manual 原因。

预期结果：

- 明确自动通过率的真实上限。
- 优先减少“OCR未识别但人工可通过”的 manual。
- 700 单耗时逐步从单机 CPU 原始耗时向 2-4 小时区间压缩；是否能低于 2 小时，需要真实数据和硬件条件验证。

---

## 10. 成本评估

### 10.1 开发成本

| 阶段 | 预计用时 |
|---|---:|
| 安全加固 | 0.5-1 天 |
| 最小闭环联调 | 1-2 天 |
| 图片角色与规则重构 | 1-2 天 |
| 本地审核服务 | 1-2 天 |
| 报表与试跑 | 2-4 天 |
| 影刀接入 | 1-3 天 |
| 首轮调优 | 2-5 天 |
| 合计 | 8.5-19 天 |

若暂不接影刀，只完成本地审核服务和本地试跑，预计 4.5-9 天。若影刀联调中遇到页面定位、下载、登录态或 JSON 解析问题，整体周期需要顺延。

### 10.2 现金成本

| 项目 | 成本 |
|---|---:|
| 本地 Python 引擎 | 0 |
| PaddleOCR/OpenCV | 0 |
| Playwright 方案 | 0 |
| 影刀 RPA | 需向影刀确认授权费用 |
| 第三方 OCR/API | 首期不使用 |

首期建议不采购云 OCR 或大模型 API，降低数据安全风险和运行成本。

---

## 11. 性能预估

### 11.1 当前事实

在桌面 `sn码测试` 文件夹 30 张图片上，现有 OCR 测试结果：

- 30 张中匹配 26 张。
- 准确率约 86.7%。
- 总耗时约 497 秒。
- 平均约 16.57 秒/张。

该测试是单张 SN 图片测试，不能直接等同于真实 700 单审核，但可作为当前 OCR 性能参考。

### 11.2 700 单耗时预估

| 模式 | 单均耗时估计 | 700 单耗时估计 |
|---|---:|---:|
| 当前命令行逐单启动 | 40-90 秒 | 8-17 小时 |
| 常驻服务 + 图片角色分类 | 25-50 秒 | 4.9-9.7 小时 |
| 常驻服务 + SN 快速命中优先 | 15-30 秒 | 2.9-5.8 小时 |
| 后续加入条码/二维码辅助并减少分块 OCR | 10-25 秒 | 1.9-4.9 小时 |

实际耗时取决于：

- 图片下载速度。
- 图片数量。
- OCR 是否经常触发分块补扫。
- 页面加载速度。
- 影刀/浏览器操作速度。
- 身份证 OCR 是否稳定。

### 11.3 性能优化边界

当前电脑为 CPU 推理环境，没有独立 GPU。性能优化不能假设引入 CLIP、视觉大模型或其他重模型，因为 CPU 推理会显著增加单单耗时。首期性能优化应集中在减少无效 OCR：

1. 图片角色先判定，再决定是否 OCR。
2. 非发券身份证图只做姓名/有效期，不做图像鉴伪。
3. 国补只对商品/拆封/SN采集图做 SN OCR 和实拍判断。
4. 优先 OCR SN码采集/激活图；命中 SN 后，不再对其他图片做分块 SN 补扫。
5. 分块 OCR 只在高价值图片上触发，不全量跑所有图片。
6. 条码/二维码本地识别作为快速辅助，能命中则减少 OCR 补扫。

如果真实试跑后仍无法满足每日时效，只有三个现实选项：

- 接受更长运行时间。
- 降低自动通过覆盖范围，只处理最标准的订单。
- 增加硬件或增加独立运行机器。

---

## 12. 实施难点、风险与解决方案

### 12.1 对《实施难点与风险分析.md》的看法

该风险分析文件的判断基本成立，尤其是以下事实不能回避：

1. 当前机器为 CPU 推理，没有独立 GPU，OCR 速度存在硬上限。
2. 现有测试集规模偏小，不能仅凭 30 张 SN、11 张身份证、19 张鉴伪图推断上线效果。
3. 影刀尚未完整联调，手脚层是当前最大工程不确定性之一。
4. 图像鉴伪误报率不能靠少量样本调参承诺降到很低。
5. 全量视觉内容识别不适合首期在当前机器上实现。

因此项目计划需要从“目标倒推”改为“实测闭环驱动”：先跑通最小链路，再用真实订单数据决定自动通过率和优化方向。

### 12.2 关键难点与落地方案

| 难点 | 事实依据 | 影响 | 解决方案 | 验收方式 |
|---|---|---|---|---|
| CPU 性能上限 | 当前 PaddleOCR 为 CPU 版；单张 SN 测试平均约 16.57 秒 | 700 单耗时可能过长 | 常驻服务预加载模型；只 OCR 必要图片；SN 命中后提前停止；限制分块 OCR 使用范围 | 记录每单耗时，统计 P50/P90/P95 |
| 分块 OCR 慢 | 分块 OCR 对小字有效，但单图可达十几秒 | 全量使用会拖慢批量审核 | 只对 SN码采集/激活图触发；商品图/拆封图只在必要时补扫；超时则 manual | 报表记录是否触发分块 OCR 及耗时 |
| SN 漏识别 | 现有 30 张测试中仍有未命中样本 | 合格单转人工，影响通过率 | 引入条码/二维码本地识别；优化 SN 正则；收集未命中样本；保留人工兜底 | 统计 manual 原因中“SN未识别”占比 |
| 实拍鉴伪误报 | 19 张鉴伪样本太少，误报率不稳定 | 合格单转人工，影响效率 | 鉴伪只用于强风险拦截；弱风险不单独阻断；先用真实样本校准阈值 | 抽查被 manual 的实拍风险订单 |
| 实拍鉴伪漏报 | 传统算法无法保证识别所有翻拍/P 图 | 可能误通过 | 自动通过只放行无强风险、SN明确匹配、图片角色完整的订单；上线初期抽检 pass 样本 | 每日抽检自动通过订单 |
| 图片内容识别不现实 | 当前无 GPU，CLIP 等模型 CPU 推理过慢 | 不适合依赖 AI 猜图片内容 | 优先使用页面图片标题；标题异常即 manual；不引入重视觉模型 | 图片角色识别准确率统计 |
| 国补交叉审核 | 下一单可能从家电切到 3C | 沿用上一单规则会误判 | 每单重新读取类型字段并动态分类；不缓存上一单品类 | 报表记录每单分类来源 |
| 影刀未联调 | 目前没有完整端到端跑通 | 上线风险高 | 先做 1 单最小闭环；再做 20 单影子试跑；最后扩大到 100-200 单 | 形成联调记录和失败清单 |
| 没有足够测试集 | 现有样本不能覆盖真实页面和真实图片 | 目标无法量化 | 建立真实样本台账：订单号、场景、品类、机器人结论、人工结论、原因 | 至少 100-200 单后再评估上线 |
| Paddle 版本锁定 | PaddlePaddle 3.2.0 受环境约束 | 升级风险 | 锁定依赖版本；新增 `requirements.lock.txt`；升级前复制环境单独测试 | 启动自检和导入测试通过 |
| Windows 中文路径/编码 | 既有脚本曾遇到中文路径和控制台编码问题 | 文件读写、日志可能异常 | 内部临时目录使用 ASCII/订单号路径；日志 UTF-8；避免命令行传中文路径 | 中文文件名样本测试通过 |
| 数据安全 | 审核数据含身份证、手机号、图片、订单信息 | 泄露后果严重 | 本地化处理；临时图片用完删除；日志只保留嘉联订单号和脱敏字段；服务只绑 127.0.0.1 | 检查日志和报表不含敏感原文 |

### 12.3 目标修正

基于上述难点，以下目标不能作为首期承诺：

- 20 秒内稳定处理每单。
- 鉴伪误报率稳定低于 10%。
- 不经真实样本验证即达到 90%+ 自动通过率。
- 使用视觉大模型自动判断所有图片内容。

首期应采用更稳妥的目标：

- 先完成最小闭环联调。
- 先影子试跑，不自动点击。
- 用 100-200 单真实数据确认基线。
- 自动通过率先以 70%-85% 为现实目标，后续根据 manual 原因优化。
- 耗时先以 4-8 小时跑完 700 单作为保守预期，再通过快路径优化争取 2-4 小时。

### 12.4 上线门槛

满足以下条件前，不建议开启自动点击“通过”：

1. 最小闭环联调成功。
2. 影子试跑不少于 100 单。
3. 自动 `pass` 样本人工抽检未发现明显误通过。
4. CSV 报表可稳定记录嘉联订单号、结论、耗时和 manual 原因。
5. 服务异常时不会点击通过。
6. 日志检查确认未保存身份证号、手机号、图片 URL、OCR 原文。

| 风险 | 影响 | 处理策略 |
|---|---|---|
| 页面结构变化 | 字段或图片读取失败 | 读取失败即 manual，不自动通过 |
| 图片标题变化 | 图片角色分类失败 | unknown 角色进入 manual |
| OCR 漏识别 SN | 合格单转人工 | 加条码/二维码、分块 OCR、样本调优 |
| OCR 相似字符误识别 | 可能误判 | SN 自动通过只接受精确匹配和明确业务例外 |
| 身份证有效期识别失败 | 合格单转人工 | 优化身份证国徽面 OCR |
| 实拍检测误报 | 合格单转人工 | 强弱风险分级，弱风险不直接阻断 |
| 实拍检测漏报 | 可能误通过 | 自动通过只放行无强风险样本，持续抽检 |
| 影刀收费或限制 | 增加成本 | 保持接口独立，可切换 Playwright |
| 影刀云端日志风险 | 数据安全风险 | 优先本地运行，确认日志/截图策略 |
| 活动规则变化 | 规则过期 | 规则配置化，人工确认后更新 |
| 电脑/家电/汽车混入 | 错误应用规则 | 非首期支持类型进入 manual |
| 国补与非发券字段差异 | 套错规则导致误判 | 先按页面栏目和活动路径识别场景 |
| 国补家电/3C交叉出现 | 沿用上一单规则导致误判 | 每单重新读取类型字段并动态分类 |
| 系统绿勾含义不明 | 误把页面标识当成业务通过 | 首期只记录，不作为决定性条件 |
| 国补发票不纳入判断 | 发票异常不由机器人处理 | 明确业务边界，机器人不负责发票 |
| 家电地址灰区 | 自动通过风险高 | 首期严格收敛，灰区 manual |

---

## 13. 基于现有项目的修改意见

本章节只基于当前 `C:\audit_robot` 项目已有结构提出改造意见，不引入脱离现状的大改。

### 13.1 当前代码现状

| 文件 | 当前职责 | 主要问题 |
|---|---|---|
| `run_audit.py` | 命令行入口、流程编排 | 场景分支较粗，图片未按角色处理，每次运行重新进程化 |
| `modules/ocr_engine.py` | PaddleOCR 封装 | 已有通用/增强/分块 OCR，但缺少按角色控制 OCR 策略 |
| `modules/code_extractor.py` | SN/IMEI 提取与匹配 | 可复用，但需要增加业务例外和条码来源 |
| `modules/id_card_parser.py` | 身份证姓名/有效期解析 | 可复用，但只能在身份证角色图片上调用 |
| `modules/image_forensics.py` | 图像鉴伪 | 当前对所有图片统一分析，不适合身份证图 |
| `modules/address_checker.py` | 地址检查 | 可作为家电辅助，但首期灰区应转人工 |
| `modules/rule_engine.py` | 规则汇总 | 需要从 `pass/skip` 改成 `pass/manual/engine_error` 语义 |
| `config.py` | OCR 和鉴伪阈值 | 需要增加安全、服务、报表配置 |

### 13.2 建议新增模块

| 新模块 | 作用 | 说明 |
|---|---|---|
| `modules/image_role.py` | 图片角色分类 | 根据页面图片标题识别身份证人像面、国徽面、商品图、拆封图、SN采集/激活图 |
| `modules/audit_models.py` | 请求/响应结构 | 统一 `AuditRequest`、`AuditResult`、`ImageItem` 等结构 |
| `modules/privacy.py` | 脱敏和日志清洗 | 统一处理身份证、手机号、SN/IMEI、图片 URL 等敏感字段 |
| `modules/report_writer.py` | CSV 报表 | 追加写入嘉联订单号、结论、耗时、manual 原因 |
| `modules/category_classifier.py` | 品类动态分类 | 每单根据类型/商品名称/品牌判断非发券、国补 3C、电脑、家电 |
| `audit_service.py` | 本地 HTTP 服务 | 常驻进程，预加载 OCR，提供 `/audit` 接口 |

这些模块都可以在现有项目内增量添加，不需要推翻当前代码。

### 13.3 `run_audit.py` 修改意见

当前 `run_audit.py` 适合作为命令行入口保留，但不建议继续承担所有业务逻辑。建议改造为：

1. 保留 `--request_file`、`--images_dir`、`--image_urls`，用于本地测试和影刀备用调用。
2. 增加对新请求结构的支持：`jl_order_no`、`scene`、`product_type`、`images: [{title, path/url}]`。
3. 先做图片角色分类，再决定每张图做什么。
4. 身份证人像面只 OCR 姓名。
5. 身份证国徽面只 OCR 有效期。
6. 商品/拆封/SN采集图做 SN OCR 和实拍检测。
7. 不再对所有图片统一跑鉴伪。
8. 输出语义改为 `pass`、`manual`、`engine_error`。

### 13.4 `ocr_engine.py` 修改意见

现有 OCR 三档策略可以保留，但需要加快路径：

1. 先 OCR SN码采集/激活图。
2. 命中系统 SN 后，停止对其他商品图做分块 SN 补扫。
3. 分块 OCR 只在系统 SN 存在、增强 OCR 未命中、当前图片角色是高价值图片时触发。
4. 身份证 OCR 不跑分块 SN 策略。

目的不是识别所有文字，而是减少不必要 OCR 耗时。

### 13.5 `image_forensics.py` 修改意见

鉴伪需要从“统一判断”改成“强弱风险输出”：

| 风险级别 | 示例 | 自动通过处理 |
|---|---|---|
| 强风险 | 明显 P 图、拼图、截图、屏幕翻拍强特征 | `manual` |
| 弱风险 | 无 EXIF、轻微模糊、单项指标边缘异常 | 不单独阻断，记录观察 |
| 无风险 | 未发现明显异常 | 可继续规则判断 |

同时必须跳过身份证图鉴伪：

- `id_front` 不鉴伪。
- `id_back` 不鉴伪。
- 仅商品/拆封/SN采集图片参与实拍判断。

### 13.6 `rule_engine.py` 修改意见

现有规则引擎可以保留，但建议新增两个入口：

1. `evaluate_no_coupon_3c(request, evidence)`
2. `evaluate_guobu_dynamic(request, evidence)`

规则结果统一为：

```json
{
  "decision": "pass/manual/engine_error",
  "action": "approve/next/stop",
  "manual_reason": "...",
  "evidence": {},
  "jl_order_no": "..."
}
```

不要再用 `skip` 表达转人工，避免和业务“驳回/跳过”混淆。

### 13.7 `code_extractor.py` 修改意见

当前 SN 精确匹配策略是正确的，不能为了提高通过率随意放宽。建议只加入明确业务例外：

- 大小写不敏感。
- 家电盒子 SN 有横杠、系统无横杠时可标准化比较。
- 苹果系统 SN 前多 `S` 的场景可作为配置项处理。
- 三星尾号 `/01` 的场景可作为配置项处理。

不建议使用编辑距离自动放行，例如 `CCP` 被识别成 `CGP` 这类不能自动通过。

### 13.8 `id_card_parser.py` 修改意见

身份证解析只用于非发券场景：

1. 人像面提取姓名。
2. 国徽面提取有效期。
3. 如果有效期无法识别，进入 `manual`。
4. 不把身份证图纳入实拍规则。

后续可增加身份证有效期专项测试集，不建议凭少量样本直接放宽。

### 13.9 `config.py` 修改意见

建议新增配置：

```python
SECURITY_CONFIG = {
    "bind_host": "127.0.0.1",
    "enable_api_token": True,
    "log_sensitive_text": False,
    "delete_temp_images": True,
}

AUDIT_CONFIG = {
    "auto_reject": False,
    "imei_required": False,
    "enable_invoice_check": False,
    "enable_energy_check": False,
    "max_seconds_per_order": 60,
}
```

这些配置用于明确边界：不自动驳回、IMEI 非强制、发票和能效不看、敏感信息不落日志。

### 13.10 改造顺序建议

推荐按以下顺序改，不建议一次性大重构：

1. 新增 `privacy.py` 和 `report_writer.py`，先解决日志安全和报表。
2. 新增 `image_role.py`，用现有图片标题做角色分类。
3. 修改 `run_audit.py`，让它按角色调用现有模块。
4. 修改 `rule_engine.py`，输出 `pass/manual/engine_error`。
5. 新增 `audit_service.py`，把命令行引擎服务化。
6. 加 100-200 单影子试跑，收集真实 manual 原因。
7. 再决定是否加条码/二维码辅助。

这样做的好处是每一步都能单独验证，失败时容易回退。

---

## 14. 相应解决措施与当前无法彻底解决的问题

本章节基于聊天确认的业务事实和 `实施难点与风险分析.md`，区分“能通过工程手段解决的问题”和“当前条件下无法彻底解决、只能兜底控制的问题”。

### 14.1 可落地解决的问题

| 问题 | 解决措施 | 实现方式 | 预期效果 |
|---|---|---|---|
| 影刀/手脚层未接入 | 先做最小闭环联调 | 1 单真实订单完成字段读取、图片下载、调用本地服务、写报表；暂不点通过 | 验证链路可行，提前暴露页面定位、下载、JSON 解析问题 |
| 每单重复加载 OCR 慢 | 改为本地常驻服务 | 新增 `audit_service.py`，服务启动时预加载 PaddleOCR，影刀/Playwright 通过 `127.0.0.1` 调用 | 减少模型重复加载时间 |
| 图片角色分类 | 不靠图片内容猜，靠页面卡片标题 | 手脚层以图片卡片为单位采集 `{title, url}`；重复标题用 `role_index` 区分 | 解决一个标题多张图、全局顺序不可靠的问题 |
| 国补家电/3C交叉审核 | 每单动态分类 | 每单重新读取 `类型/商品名称/品牌/规格型号`，不能沿用上一单规则 | 避免家电和 3C 套错规则 |
| 身份证图被误判非实拍 | 身份证图片跳过鉴伪 | `id_front/id_back` 只做姓名/有效期 OCR，不跑图像鉴伪 | 降低非发券误转人工 |
| IMEI 非强制 | IMEI 改为辅助证据 | 识别到且一致则加分；识别不到不阻断；识别到明显冲突则 manual | 符合业务规则，提高自动通过率 |
| SN 小字 OCR 慢 | 快路径 + 分块限制 | 优先 OCR SN采集/激活图；命中后停止补扫；分块只对高价值图触发 | 降低平均耗时 |
| 商品/包装不能完全靠 OCR 判断 | 构建证据链 | 页面品类 + 图片标题 + SN命中 + 品牌/型号/商品条码/SN标签 OCR + 条码/二维码 + 非实拍风险 | 不做万能视觉识别，但让标准单具备足够通过证据 |
| 家电地址必须小范围 | 本地地址粒度规则 | 判断地址是否到小区/路/号/栋/单元/室，农村是否到村/组；只到省市区县/乡镇则 manual | 可规则化处理地址粗细 |
| 日志数据安全 | 脱敏与最小留存 | 嘉联订单号完整保留；身份证、手机号、图片 URL、OCR 原文不落日志；临时图片用完删除 | 降低敏感数据泄露风险 |
| 没有真实度量 | 影子试跑 + CSV 报表 | 先跑 100-200 单，只记录建议结果，不点击通过 | 得到真实自动通过率、耗时和 manual 原因分布 |

### 14.2 当前无法彻底解决的问题

| 问题 | 为什么无法彻底解决 | 当前处理策略 |
|---|---|---|
| CPU 推理速度上限 | 当前电脑无独立 GPU，PaddleOCR 全靠 CPU；重视觉模型不现实 | 降低无效 OCR、常驻服务、快路径；若仍不达标，只能接受更长时间或增加硬件/机器 |
| SN OCR 100% 命中 | 小字、反光、模糊、角度、遮挡会导致 OCR 漏识别 | SN 未高置信命中则 manual；后续用条码/二维码和失败样本调优 |
| 商品和包装的通用视觉理解 | OCR 不理解图像内容；CPU 跑 CLIP/视觉大模型过慢且不稳定 | 不做万能识别；用证据链自动通过标准单，证据不足转人工 |
| 图像鉴伪 100% 准确 | 传统算法无法覆盖所有翻拍/P 图，也会误报真实图 | 只把强风险转 manual；弱风险记录观察；上线初期抽检 pass 样本 |
| 自动通过率直接达到 90%+ | 现有测试集太小，真实页面和图片复杂度未知 | 不承诺上线即 90%+；先测真实基线，再按 manual 原因优化 |
| 影刀收费和云日志策略 | 取决于影刀产品和授权政策，当前未实测确认 | 保持本地服务接口独立；若影刀成本或安全不满足，切换 Playwright |
| 页面未来改版 | 审核后台 DOM、图片标题、按钮位置可能变 | 读取失败即 manual/暂停；报表记录异常；手脚层需要维护 |
| 家电地址灰区 | “到村可过”和“乡村需到组”存在业务解释差异 | 首期保守：到组/号/栋/单元/室自动通过；只到村先 manual，待业务确认后放宽 |
| 发票和能效异常 | 当前业务要求机器人不看发票和能效 | 明确边界：机器人不负责此项；如页面存在其他强风险仍转人工 |

### 14.3 商品/包装证据链方案

为避免“每单都必须人工看产品和包装”，首期不做通用视觉理解，但对标准单建立可计算证据链。

自动通过需要满足：

1. 页面品类在支持范围内。
2. 图片标题完整：商品照片、拆封照片、SN码采集/激活照片。
3. 系统 SN 在至少一张商品相关图片中被明确识别。
4. 商品相关图片无强非实拍风险。
5. 至少一类辅助证据命中：
   - 品牌 OCR 命中；
   - 规格型号 OCR 命中；
   - 商品条码/条形码命中；
   - SN 标签/铭牌类文字命中；
   - 条码/二维码解析出可用编号。
6. 图片不明显重复。

证据链完整的订单可自动通过；证据不足但不一定错误的订单进入 manual。

### 14.4 家电地址合规方案

家电地址采用本地粒度规则，不调用外部地图 API。

地址判断分层：

| 地址粒度 | 示例 | 处理 |
|---|---|---|
| 明显详细 | 小区、路、号、栋、单元、室、门牌、村、组 | 可作为自动通过条件之一 |
| 中等模糊 | 到乡镇/街道/村，但没有组/号/栋/门牌 | 首期 manual |
| 明显过粗 | 只到省/市/区/县 | manual |

家电自动通过不能只看地址，还必须同时满足 SN 匹配、图片角色齐全、商品相关图片无强风险。

### 14.5 上线策略

为避免把未解决问题带入生产，采取分阶段开关：

1. `observe_only`：只记录建议，不点击。
2. `approve_only`：只自动点通过，manual 点下一条。
3. `strict_mode`：任何字段缺失、图片角色异常、服务超时、OCR 不确定都 manual。
4. `rollback`：发现误通过，立即关闭自动点击，只保留报表。

---

## 15. 后续扩展方向

### 13.1 条码/二维码辅助

包装盒或商品标签上可能存在条形码/二维码。可引入本地开源库作为 OCR 辅助，优先用于 SN 或商品条码识别。

候选技术：

- zxing-cpp
- pyzbar

该能力不上传数据，适合本地化。

### 13.2 家电审核模块

后续可新增：

- 地址细化判断。
- 能效/水效标签识别。
- 家电 SN 特殊格式处理。
- 家电商品/拆封照片角色判断。

### 13.3 Playwright 手脚层

若影刀成本或安全策略不满足要求，可使用 Playwright Python 替代影刀。由于本地审核服务接口独立，替换手脚层不会影响核心引擎。

### 13.4 汽车审核辅助

汽车不建议首期自动通过。后续可以先做辅助识别：

- 行驶证 OCR。
- 车牌号识别。
- 车架号识别。
- 银行卡号识别。
- 发票/小票字段提取。

先给人工提示，不直接自动通过。

---

## 16. 推荐落地路线

推荐采用“小步快跑”的路线：

1. 先完成 1 单最小闭环联调，证明页面读取、图片下载、引擎调用、报表记录能跑通。
2. 完成本地服务化和非发券 3C/国补动态分类规则。
3. 影子试跑 100-200 单，只生成建议和报表，不点通过。
4. 抽查自动 `pass` 样本，确认无明显误通过后，再开启自动点通过。
5. 所有 `manual` 订单只点下一条，不自动驳回。
6. 用真实报表持续优化自动通过率和耗时。
7. 待非发券 3C 与国补标准单稳定后，再扩展家电灰区能力。
8. 汽车单独规划。

首期最重要的不是功能多，而是：

- 安全。
- 稳定。
- 不误通过。
- 真实提高效率。
