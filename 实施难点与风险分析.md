# 审核机器人实施计划 — 难点与风险分析

> 实事求是，不理想化。基于所有实测数据和已知约束。

---

## 一、根本性约束：你的电脑配置

| 配置项 | 数值 | 对项目的影响 |
|--------|------|-------------|
| CPU | Intel i5-14400 (10核16线程, 2.5GHz) | 够用，但不快 |
| RAM | 16GB DDR4 3200 | 够用 |
| GPU | Intel UHD Graphics 730（集成显卡） | **无法用于AI加速** |
| 硬盘 | 1TB SSD | 够用 |
| PaddlePaddle | CPU版 3.2.0 | 纯CPU推理，无法启用GPU加速 |

**结论：** 你的电脑配置正常，跑得动，但速度上限就卡在这里了。没有独显意味着：
- PaddleOCR 全靠 CPU 算，一张增强 OCR 要 3 秒，分块要 17 秒
- 不可能跑任何视觉 AI 模型（CLIP 等），CPU 推理一张图就要 30 秒
- 这不是写代码能解决的问题，是硬件天花板

---

## 二、最核心的矛盾：速度 vs 预算

### 事实数据
- 单张 OCR 平均：增强 3s + 分块 17s（仅未命中时触发）
- 每单 3 张图，约 66% 的图需要分块补扫
- **每单实际耗时：约 45-70 秒（实测）**
- 你提过的时间要求：**约 20 秒/单**

### 差距有多大
- 如果 500 单全部跑完：最长 **~8 小时**，最短 **~4 小时**（你的电脑要一直转）
- 如果要求 20 秒/单：目前差了 **2-3 倍**
- **这是最硬的问题，没有好办法绕过**

### 可能的折中
| 方案 | 每单耗时 | 代价 |
|------|---------|------|
| 保持现状（增强+分块） | ~50s | 最多，最准 |
| 去掉分块OCR，只用增强 | ~10s | SN命中率从93%掉到~28% |
| 限制分块：只对第3张图做分块 | ~25s | 漏掉1、2张上的小字SN |
| 减少分块分辨率（960→800） | ~35s | 小字识别可能下降 |

**没有免费的午餐。** 要么牺牲速度，要么牺牲准确率。

---

## 三、过于理想化的目标

### 1. "鉴伪误报率降到 10%"

**为什么理想化：**
- 只有 19 张测试图，其中 6 张误报，样本太小
- 调阈值在这 19 张上"调好了"，换一批图可能完全不一样
- 10% 是专业图像检测团队用大量标注数据才能达到的水平
- **更现实的目标：15-20%**，且需要你收集 50-100 张真实照片做验证

### 2. "图片内容识别（分辨手机 vs 电视 vs 包装盒）"

**为什么理想化：**
- 这需要 CLIP 或类似视觉语言模型
- 你的电脑没有 GPU（只有集成显卡 UHD 730），CPU 推理一张图 30 秒起步
- 3 张图就是 90 秒，500 单就是 **45 小时**——不现实
- 还要装 pytorch/transformers 等依赖，容易和现有 PaddlePaddle 冲突
- **更现实的方案：让影刀在调用时标注"第1张是激活照"，引擎不需要猜**

### 3. "全自动通过率 90%+"

**为什么理想化：**
- SN 精确匹配的命中率是 93%（26/28），这意味着 7% 的图 OCR 读不出 SN
- 如果 7% 的图读不出 SN，至少 7% 的订单要转人工（只看 SN 这一项）
- 加上鉴伪误报 ~20%，加上地址异常、IMEI 不匹配等
- **更现实的目标：500 单中 380-420 单自动通过（76-84%）**

### 4. "每天几百单全自动跑不用管"

**为什么理想化：**
- 每单 ~50s，500 单连续跑就是将近 7 小时
- 这 7 小时内你的电脑基本在做 OCR，不能同时做其他重活
- 影刀和引擎是独立进程，但都占 CPU 资源
- **更现实的情况：你白天正常用电脑，引擎在后台跑，单量可能跑不完**

---

## 四、技术风险

### 1. PaddlePaddle 版本锁定
- 必须锁在 3.2.0，不能升 3.3.1（有 PIR bug）
- 这意味着后续如果想升级 PaddleOCR 也可能受限制
- **暂时无解，除非 PaddlePaddle 修了那个 bug**

### 2. Windows 中文编码问题
- 所有 .py 文件必须带 `# -*- coding: utf-8 -*-`
- 路径中的中文/空格可能引发问题
- **目前的代码已处理，但后续修改要注意**

### 3. 影刀集成不确定
- 引擎的输入输出接口已经定义好（JSON in/out）
- 但影刀那边怎么调用、怎么解析 JSON、怎么处理异常——**这部分还没做过一次完整的联调**
- **联调时大概率会遇到意料之外的问题**

### 4. 没有测试集
- 现有的测试数据（30 张 SN 图、11 张身份证）远远不够
- 没有"标准答案"（ground truth），没法量化每次改动的好坏
- **这是个隐患：没有度量就没有改进**

---

## 五、实际可行的实施步骤（调整后）

### 第一步（最优先）：跟影刀做一次完整联调
- 确认 URL 传图能不能正常下载
- 确认 JSON 输出能不能被影刀正确解析
- 确认 pass/skip 的后续操作（点击、跳转）
- 预计需要：**你和负责影刀的人一起配合**

### 第二步：加 CSV 报表
- 加 `--report` 参数，每单追加一行 CSV
- 这样你每天能看到：多少 pass、多少 skip、skip 原因分布
- 纯代码改动，**1 次会话完成**

### 第三步：鉴伪阈值初步调优
- 先按现有数据调一轮，把明显不合理的误报修掉
- 之后每天看 CSV 报表，根据实际数据微调
- **持续优化，没有终点**

### 第四步：堆时间跑量
- 每天让引擎跑，你处理转人工的单
- 积累足够多的实际数据后，再决定要不要优化小字 SN 等问题
- **可能跑几天你会发现新的问题，比现在纸上谈兵更准确**

---

## 六、实话实说总结

| 问题 | 结论 |
|------|------|
| 引擎能不能用？ | **能用了现在。** 80% 单自动过，不放过问题单 |
| 速度够不够？ | **不够。** 20s/单的预算是达不到的，实际 50s 左右 |
| 电脑行不行？ | **行，但慢。** 没有 GPU 是硬伤 |
| 鉴伪误报能解决吗？ | **能改善，但不太可能低于 15%** |
| 图片内容识别能上吗？ | **目前不建议。** 没 GPU 跑不动，收益不确定 |
| 最终能自动过多少？ | **稳定在 80-85% 是现实目标** |
| 最大的风险是什么？ | **跟影刀的联调和速度瓶颈** |

*文档生成日期：2026-05-20*
*基于 30 张 SN 测试 + 11 张身份证测试 + 19 张鉴伪测试的实际数据*
